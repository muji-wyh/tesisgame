# Adventure book implementation plan

1. Extend the model reset with validated explicit topic selection; retain existing repeat and seeded behavior.
2. Extend personal-choice persistence with optional visit history and preferred world, preserving existing records and failure recovery.
3. Build a responsive illustrated adventure book using existing art and normal Godot buttons.
4. Integrate Explore and New adventure with the existing collection modal, save guards, pause and focus restoration. Start selected destinations in Learn.
5. Verify native state/model/scene behavior and browser learning, selection, scroll, cancel, persistence and reward regressions. Review the changes independently.
6. Commit, fast-forward main, push, deploy the tested web export and verify production.
